# tests

Home for lightweight regression checks and script-specific validation helpers.

When the repo starts accumulating reusable logic under `src/`, new tests should target those helpers first rather than only exercising the root wrappers.
